## 1. 查询筛选控件状态一致性

- [x] 1.1 调整列表局部刷新后的筛选控件初始化逻辑，确保普通下拉和区域控件都会重新增强。
- [x] 1.2 调整区域控件的空态展示逻辑，未选择时使用占位提示而不是把占位文案写成真实值。

## 2. 区域下拉展示修复

- [x] 2.1 调整区域下拉面板样式，使其不受输入框宽度限制并能完整展示长区域名称。
- [x] 2.2 校验共享筛选样式，确保区域控件空态颜色与其他后台下拉保持一致。

## 3. 验证

- [x] 3.1 在青年信息列表页验证初次加载、查询后、重置后和分页后的筛选控件显示状态。
- [x] 3.2 检查共享脚本和样式改动未引入明显的前端诊断问题。
