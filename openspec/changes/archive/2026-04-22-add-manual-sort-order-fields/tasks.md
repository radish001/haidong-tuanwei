## 1. 数据模型与后端映射

- [x] 1.1 在 `schema.sql` 中为 `youth_info`、`enterprise_info`、`job_post`、`policy_article` 新增可空 `sort_order` 字段，并整理实际环境需要执行的数据库变更语句
- [x] 1.2 为青年、企业、招聘、政策四类实体与表单 DTO 增加 `sortOrder` 字段，并补齐 Controller `toForm(...)` 与 Service `toEntity(...)` 的双向映射
- [x] 1.3 更新四个模块的 MyBatis `resultMap`、`findById`、`insert`、`update` 查询，确保排序字段可正确保存、查询和回显

## 2. 列表排序与匹配结果

- [x] 2.1 调整青年、企业、招聘、政策四个主列表查询的排序规则为“已填排序优先、按排序升序、同值按 `id desc`、空值按 `id desc`”
- [x] 2.2 调整岗位匹配学生与学生匹配岗位结果查询，确保匹配结果页继承对应实体的人工排序规则
- [x] 2.3 移除青年、企业、招聘、政策主列表以及两个匹配结果列表中的“序号”列，并保持分页信息与其他业务列正常展示

## 3. 表单与详情页展示

- [x] 3.1 在青年、企业、招聘、政策四个新增/编辑表单中新增“排序”输入项，允许为空且使用整数输入语义
- [x] 3.2 在青年、企业、招聘、政策四个详情页中展示“排序”字段，并保持详情页只读

## 4. 验证与收尾

- [x] 4.1 更新或补充受影响模块的测试，覆盖排序字段保存回显、列表排序规则以及匹配结果排序规则
- [x] 4.2 手动验证青年、企业、招聘、政策主列表及青年/招聘匹配结果页的排序表现、空值兜底行为和“序号”列移除效果
