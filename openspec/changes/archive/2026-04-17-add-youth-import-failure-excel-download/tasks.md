## 1. 导入结果建模

- [x] 1.1 扩展青年导入结果 DTO，保存失败行的原始列值、失败原因和原始行号
- [x] 1.2 调整青年导入流程，在逐行校验失败时同步收集失败行原始数据，继续保留现有页面失败明细能力

## 2. 失败数据 Excel 生成与下载

- [x] 2.1 基于现有青年导入模板生成逻辑实现失败数据 Excel，复用隐藏 sheet、命名区域和下拉选项，并在末尾追加 `失败原因` 列
- [x] 2.2 新增青年失败数据 Excel 下载接口，并在存在失败数据时返回仅包含失败行的返修文件

## 3. 导入兼容与页面入口

- [x] 3.1 调整青年导入表头校验和解析逻辑，兼容末尾附带 `失败原因` 列的返修文件并在导入时忽略该列
- [x] 3.2 在青年导入结果区域增加“下载失败数据”入口，仅在本次导入存在失败行时展示

## 4. 验证

- [x] 4.1 补充服务层测试，覆盖失败行原始值收集、失败 Excel 生成和带 `失败原因` 列文件再次导入
- [x] 4.2 补充控制器/集成测试，覆盖失败下载入口展示与失败数据 Excel 下载行为
