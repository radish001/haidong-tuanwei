## 1. 数据与静态资源准备

- [x] 1.1 补齐 `sys_region` 所需的中国省级区域种子数据，确保学校所在省统计具备完整主数据
- [x] 1.2 补齐海东市 2 区 4 县的区域种子数据，并校验区县名称与目标地图资源一致
- [x] 1.3 准备并落库首页使用的中国省级 GeoJSON 与海东市区县 GeoJSON 静态资源

## 2. 首页地图数据聚合

- [x] 2.1 在 `DashboardDao` 与对应 MyBatis XML 中新增中国地图按学校所在省聚合在校大学生分布的查询
- [x] 2.2 在 `DashboardDao` 与对应 MyBatis XML 中新增海东地图按籍贯区县聚合在校大学生分布的查询
- [x] 2.3 在 `DashboardService` 与 `DashboardController` 中装配两张地图的数据，并以首页模板可直接消费的形式输出

## 3. 首页地图渲染

- [x] 3.1 调整 `dashboard/index.html`，用两个固定地图卡片替换当前按青年分类循环的占位分布卡片
- [x] 3.2 在首页接入 `ECharts` 地图初始化脚本，分别渲染中国地图和海东地图
- [x] 3.3 为首页地图补充明确统计口径的标题或说明文案，并处理空数据时的展示兜底

## 4. 验证

- [x] 4.1 验证中国地图仅统计 `youth_type = 'college'` 且按学校所在省展示分布
- [x] 4.2 验证海东地图仅统计 `youth_type = 'college'` 且按海东市籍贯区县展示 2 区 4 县分布
- [x] 4.3 验证地图区域名称与 GeoJSON 资源可正常匹配，页面加载后两张地图均能稳定渲染
