## 1. 镜像与目录结构

- [x] 1.1 新增 `docker/` 目录，并创建应用镜像所需的多阶段 `Dockerfile`。
- [x] 1.2 补充容器运行所需的环境变量示例和目录约定，明确日志、上传文件和数据库数据的持久化路径。

## 2. Compose 运行环境

- [x] 2.1 编写包含 `app` 和 `mysql` 两个服务的 `docker-compose.yml`，让应用通过现有 `MYSQL_*` 配置连接 Compose 中的 MySQL。
- [x] 2.2 为 MySQL 配置健康检查，并为应用添加依赖等待与端口、卷挂载配置，确保 `docker compose up -d` 可以稳定拉起环境。

## 3. 文档与验证

- [x] 3.1 更新部署文档，说明如何构建和启动容器环境、访问应用，以及数据库表结构和初始化数据仍需后续手工导入。
- [x] 3.2 进行一次容器化启动验证，确认 Compose 可以成功拉起应用与 MySQL，并记录必要的使用说明或已知限制。
